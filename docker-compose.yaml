version: '3'

services:
  nginx:
    image: nginx:latest
    container_name: nginx-playground
    ports:
      - "80:80"
    volumes:
      - ./nginx-config:/etc/nginx/conf.d
      - ./logs:/var/log/nginx/
    restart: always